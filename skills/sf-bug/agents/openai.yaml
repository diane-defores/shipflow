interface:
  display_name: "sf-bug"
  short_description: "Orchestrate bug intake and closure"
  default_prompt: "Use $sf-bug to route bug intake, bug files, fixes, retests, verification, and ship risk."
