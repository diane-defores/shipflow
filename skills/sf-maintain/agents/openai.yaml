interface:
  display_name: "sf-maintain"
  short_description: "Run maintenance through verification and ship"
  default_prompt: "Use $sf-maintain to triage maintenance, create or continue a spec when needed, delegate fixes, verify, and route ship."
