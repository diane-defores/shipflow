interface:
  display_name: "004-sf-deploy"
  short_description: "Deploy and verify a release"
  default_prompt: "Use $004-sf-deploy to check, ship, wait for deployment, and route post-deploy verification."
