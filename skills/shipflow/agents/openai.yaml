interface:
  display_name: "shipflow"
  short_description: "Route a plain-language request to the right ShipFlow skill"
  default_prompt: "Use $shipflow with a natural-language instruction to answer directly or hand off to the right ShipFlow skill."
