---
import { homeCopy, pathFor, type Locale } from "../i18n/ui";

interface Props {
  locale?: Locale;
}

const { locale = "en" } = Astro.props;
const copy = homeCopy[locale].hero;
---

<section class="hero">
  <div class="hero-copy">
    <p class="eyebrow">{copy.eyebrow}</p>
    <h1>{copy.title}</h1>
    <p class="hero-text">{copy.body}</p>
    <ul class="hero-points">
      {copy.points.map((point) => <li>{point}</li>)}
    </ul>
    <div class="hero-actions">
      {copy.actions.map(([label, href, className]) => <a class={className} href={pathFor(href, locale)}>{label}</a>)}
    </div>
  </div>

  <div class="hero-panel">
    <div class="hero-map">
      {copy.blocks.map(([label, body]) => (
        <div class="hero-map-block">
          <span>{label}</span>
          <strong>{body}</strong>
        </div>
      ))}
      <div class="hero-map-note">
        {copy.note}
      </div>
    </div>
  </div>
</section>
